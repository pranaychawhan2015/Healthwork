# Medical_Records


Please first go to healthwork - run ./startFabric.sh javascript - This will generate the test-network with the chaincode at path chaincode/healthwork.

After that go to healthwork/javascript folder - run node app.js - This will start the local server of nodejs for the dapp

go to healthwork-ui folder - run npm start - This will start the frontend app.
